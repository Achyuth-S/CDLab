#include <stdio.h>

int main() {
    int x;
    printf("Enter a number: ");  // This should be replaced with WRITE
    scanf("%d", &x);  // This should be replaced with READ

    printf("You entered: %d\n", x);  // This should be replaced with WRITE
    return 0;
}